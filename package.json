{
  "name": "budget",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler pages dev . --compatibility-date=2024-06-20",
    "build": "wrangler pages deploy . --dry-run --commit-dirty=true"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241112.0",
    "typescript": "^5.3.3",
    "wrangler": "^3.88.0"
  }
}
